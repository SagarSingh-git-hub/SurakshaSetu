# Database Configuration
DB_HOST=localhost
DB_USER=root
DB_PASS=
DB_NAME=eco_warrior
DB_PORT=3307

# Admin Configuration
ADMIN_PASSWORD=your_secure_password

# Pusher Credentials
PUSHER_APP_ID=YOUR_PUSHER_APP_ID
PUSHER_KEY=YOUR_PUSHER_KEY
PUSHER_SECRET=YOUR_PUSHER_SECRET
PUSHER_CLUSTER=YOUR_PUSHER_CLUSTER

# Frontend URL (For CORS - use https://yourdomain.com in production)
FRONTEND_URL=https://suraksha-setu-chi.vercel.app/

# Environment Mode (development or production)
APP_ENV=development
